name: "ci workflow"
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name:"starting ci workflow"
        run:echo "starting ci workflow"
      